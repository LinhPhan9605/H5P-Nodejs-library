user nginx;

events {
    worker_connections   1000;
}
http {
    client_max_body_size 1024M;
    server {
            listen 3009;
            location / {
            proxy_pass http://h5p:3009;
            }
    }
}